name: coffee
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8
  path: ^1.9.1
  path_provider: ^2.1.5
  easy_localization: ^3.0.8


dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0
  build_runner: ^2.10.1
  drift_dev: ^2.29.0

flutter:

  uses-material-design: true

  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg

  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
scripts:
  clean: >
    flutter clean
    flutter pub get
#  generate local database. it need two packages: build_runner & drift_dev
#  and than write table.drift & database.dart and run this scripts
#  if in database need path_provider, sqlite3 or sqlcipher_flutter_libs. we need import this package
  build: dart run build_runner build --delete-conflicting-outputs # only testing right now
  locale: >
    dart run easy_localization:generate -S assets/languages -f keys -o locale_keys.g.dart 
    flutter pub run easy_localization:generate -S assets/languages

